apiVersion: apps/v1
kind: Deployment
metadata:
  name: vector-web-underwriting
  namespace: vector
  labels:
    app.kubernetes.io/name: vector-web-underwriting
    app.kubernetes.io/component: web
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: vector-web-underwriting
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vector-web-underwriting
        app.kubernetes.io/component: web
    spec:
      serviceAccountName: vector-web
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: web
          image: vector-web-underwriting:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: vector-config
            - secretRef:
                name: vector-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health/live
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health/ready
              port: http
            initialDelaySeconds: 10
            periodSeconds: 5
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: vector-web-underwriting
  namespace: vector
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: vector-web-underwriting
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vector-web
  namespace: vector
